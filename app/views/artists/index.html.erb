<br><br><br><br><br>
<div class="results justify-content-center w-75 h-100 bg-light m-auto">
<h2><%= @artist.name %></h2>  
  <h4>Artist Bio( from last.fm api)</h4>
  <p><%= @artistbio %></p>

<h4>Top Tracks(spotify api)</h4>
  <ul>
    <% @tracks[0..4].each_with_index do |track, i| %>
      <li>
        #<%= i += 1 %> : <%= track.name %>(spotify api)
        
        
      </li>
    <% end %>
  </ul>
  </div>